class Solution(object):
    def plusOne(self, digits):
        result = 0
        for i in digits:
            result = result * 10 + i
            final = result+1
            to_list= list(str(final))
            print(to_list)
        ans = [int(digit) for digit in to_list]
        return ans
        
